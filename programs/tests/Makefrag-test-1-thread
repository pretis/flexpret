# Specify benchmark tests for certain ISA.
# FIXME: Add more tests
rvi-bmark-tests = \
	mediam.riscv \
	multiply.riscv

# Add "output_dir" as prefix (i.e. putting the file in output_dir),
# and add ".imem.hex" as postfix
# FIXME: Understand what the addprefix... target does.
# suffix = .imem.hex
# $(addprefix $(output_dir)/, $(addsuffix $(suffix), $(rvi-bmark-tests))): $(output_dir)/%$(suffix):

#--------------------------------------------------------------------
# Run tests 
#--------------------------------------------------------------------
